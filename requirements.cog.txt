numpy<2
# torch version matched to base; Cog base image python3.10-torch2.4.0 already includes torch
# if Cog base changes, uncomment next line or set a compatible version
# torch==2.4.0
transformers>=4.56.0
accelerate>=1.0.0
pillow>=10.0.0
safetensors>=0.4.0
huggingface-hub>=0.20.0
hf_transfer>=0.1.7
# diffusers 0.35 is required for Qwen Image pipeline
# 0.35 pulls torchao quantizer dependency paths; compatible with torch 2.4
# if needed, pin precisely:
# diffusers==0.35.0
diffusers>=0.35.0
